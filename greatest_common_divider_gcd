def gcd(x,y):
   if y==0:
      print(x)
   else:
   gcd(y,x%y)
   
   
def gcd2(a,b):
    gcd = 1
    for i in range(2,min(a,b)):
        gcd = i
    print(gcd)
    
    
    
def gcd3(a,b):
print(max([i for i in range(1,min(a,b)+1) if a%i==0 and b%i==0]))
